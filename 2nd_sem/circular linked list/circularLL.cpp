#include <iostream>
using namespace std;

struct Node {
    int val;
    Node* next;

    Node(int v) {
        val = v;
        next = NULL;
    }
};

void createLL(Node* &head) {
    
}

int main() {
    Node* head = NULL;
}